# Token source note

The admin/control panel ("Horizon" design system, `--hz-*` CSS custom properties on :root)
is distinct from the customer-facing community theme (`--config-*` variables, which are
tenant-brand-editable: purple #9254d9 brand color etc).

We only care about --hz-* for control-side prototypes.
